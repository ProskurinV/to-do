export const Title = ({ title }) => {
  return (
    <h1
      style={{
        display: 'flex',
        justifyContent: 'center',
        marginBottom: 20,
        marginTop: 20,
      }}
    >
      {title}
    </h1>
  );
};
